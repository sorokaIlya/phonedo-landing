{
  "name": "template-new",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "sass": "^1.93.2",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "@flaticon/flaticon-uicons": "^3.3.1"
  }
}
